# Slack Bot Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack App Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token

# Claude Code OAuth Token (from `claude setup-token`)
# Use this if you have a Claude Max subscription
#
# OPTION 1: Single token (simplest)
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token
#
# OPTION 2: Multiple tokens with aliases (for switching between accounts)
# Format: CLAUDE_CODE_OAUTH_TOKEN_<alias>=<token>
# The first token becomes the default, or use "default" as alias
# Examples:
# CLAUDE_CODE_OAUTH_TOKEN_default=sk-ant-oat01-your-default-token
# CLAUDE_CODE_OAUTH_TOKEN_work=sk-ant-oat01-your-work-token
# CLAUDE_CODE_OAUTH_TOKEN_personal=sk-ant-oat01-your-personal-token
#
# Users can switch tokens via DM:
#   - "tokens" - List available tokens
#   - "use <alias>" - Switch to a different token
#
# OR Anthropic API Key (if using direct API access)
# ANTHROPIC_API_KEY=sk-ant-your-api-key

# Port for internal orchestrator communication (default: 3000)
ORCHESTRATOR_PORT=3000

# Working directory for Claude Code (default: current directory)
# WORKING_DIRECTORY=/path/to/your/project

# Claude model to use (default: claude-sonnet-4-5-20250929)
# Options: claude-opus-4-5-20251101, claude-sonnet-4-5-20250929, claude-haiku-4-5
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Context window max tokens (default: 200000)
# Used to calculate percentage of context used in /context command
CONTEXT_WINDOW_MAX=200000

# Shared cache for large files (HuggingFace models, PyTorch, etc.)
# Update paths to match your installation directory
HF_HOME=/path/to/claude-code-minion/projects/.cache/huggingface
TRANSFORMERS_CACHE=/path/to/claude-code-minion/projects/.cache/huggingface
TORCH_HOME=/path/to/claude-code-minion/projects/.cache/torch
