{
  "name": "claude-code-minion",
  "version": "1.0.0",
  "description": "Slack bot that connects to Claude Code running on a remote server",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.mcp.json",
    "build:main": "tsc",
    "build:mcp": "tsc -p tsconfig.mcp.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@slack/bolt": "^3.17.0",
    "@slack/web-api": "^7.0.0",
    "node-pty": "^1.0.0",
    "strip-ansi": "^7.1.0",
    "uuid": "^9.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "pm2": "^6.0.14",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
